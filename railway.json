{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "volumeMounts": [
      {
        "name": "llama-models-volume",
        "mountPath": "/opt/models/.cache/huggingface"
      }
    ]
  }
}